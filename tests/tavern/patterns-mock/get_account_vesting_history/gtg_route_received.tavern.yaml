---
  test_name: btracker PostgREST

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: test
      # issue #54 (positive, auto_vest=false): the fill_vesting_withdraw routed
      # blocktrades -> gtg must appear here under power_down_route_received as the
      # deposited HIVE (7.000 HIVE), NOT under power_down_fill.
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/rpc/get_account_vesting_history"
        method: POST
        headers:
          content-type: application/json
          accept: application/json
        json:
          account-name: "gtg"
          filter: "power_down_route_received"
          from-block: "90000054"
          page-size: 100
          direction: "desc"
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_rest_response_with_pattern
