[POSTGRES]
database = haf_block_log # Name of postgres database (e.g. "haf_block_log")
user = api_user # User with database access rights
password = api_user # User login password
host = 127.0.0.1 # Host address where postgres database is
port = 5432 # Postgres port for connection